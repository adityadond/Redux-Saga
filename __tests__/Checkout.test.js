import React from "react";
import { render } from "@testing-library/react";
import Checkout from "../src/Checkout";

jest.mock("../src/CheckOut.css", () => ({}));

describe("Checkout Component", () => {
  test("renders CheckoutStepper with correct steps", () => {});
});
